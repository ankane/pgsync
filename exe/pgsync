#!/usr/bin/env ruby

trap("SIGINT") { abort }

begin
  require "pgsync"
  PgSync::Client.new(ARGV).perform
rescue PgSync::Error => e
  abort PgSync::Client.colorize(e.message, 31) # red
rescue Interrupt
  abort
end
